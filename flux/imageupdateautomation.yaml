apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: automation
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: spring-petclinic-config
  update:
    path: ./flux  # Ajusta esta ruta al lugar correcto dentro de tu repo2
    strategy: Setters
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        email: david.perez945@comunidadunir.net
        name: david-unir
      messageTemplate: "ci: update images"  # Usa comillas dobles aquí para evitar problemas con los caracteres especiales.
    push:
      branch: main